import 'package:flutter/material.dart';
class MenuProtocolo extends StatelessWidget {
   const MenuProtocolo({ Key? key }) : super(key: key);



  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      appBar: AppBar(title: const Text('Testando')),
      body: Container(),
    );
  }
}