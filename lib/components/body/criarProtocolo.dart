import 'package:flutter/material.dart';

class EditarProtocolo extends StatelessWidget {
  const EditarProtocolo({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Criar'),
    );
  }
}